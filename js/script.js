var studentAge = 17;

var livingCity = "Xirdalan";

var isMarried = false;

studentAge++; // 18
studentAge--; // 17
studentAge--; // 16

studentAge += 10; // 26

// studentAge -= 10; // 16


console.log(studentAge >= 18 && (livingCity == "Baki" || livingCity == "Sumqayit" || livingCity == "xirdalan"))